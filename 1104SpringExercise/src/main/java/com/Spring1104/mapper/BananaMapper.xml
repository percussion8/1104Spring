<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	 
<mapper namespace="com.Spring1104.mapper.BananaMapper">
	<select id="select" resultType="com.Spring1104.domain.BananaVO">
		<![CDATA[select no, name, id from banana
		]]>
	</select>
	<insert id="insert">
		insert into banana (no, name, id) values (AAA.nextval, #{name}, #{id})
	</insert>
	<update id="update">
		update banana set name=#{name}, id=#{id} where no=#{no}
	</update>
	<delete id="delete">
		delete from banana where no = #{no}
	</delete>
	<select id="read" resultType="com.Spring1104.domain.BananaVO">
		select no, name, id from banana where id = #{id}
	</select>



</mapper>